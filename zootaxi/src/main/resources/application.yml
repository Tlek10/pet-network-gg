server:
  port: 8084
  servlet:
    context-path: /

spring:
  application:
    name: zootaxi-service
  datasource:
    url: jdbc:postgresql://localhost:5435/pet_social_network_gg
    username: admin
    password: password
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

logging:
  level:
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql: TRACE
    com.pet.social: DEBUG
