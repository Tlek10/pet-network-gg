spring:
  datasource:
    url: jdbc:postgresql://localhost:5435/pet_social_network_gg
    username: admin
    password: password
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  application:
    name: auth-service

logging:
  level:
    root: INFO
    org.springframework.security: DEBUG
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql: TRACE
    com.pet.social: DEBUG

server:
  port: 8081
